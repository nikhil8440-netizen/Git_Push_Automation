# Node / Electron build artifacts
node_modules/
out/
dist/
*.tsbuildinfo

# Editors
.vscode/
.idea/
*.log

# Personal / machine-specific data — never commit.
# The app stores these in the OS userData directory; these patterns are a
# safety net so a stray copy in the repo is never pushed.
config.json
logs.json
*.corrupt
.lock
build/icon-preview-256.png

shortcut/

# Internal docs / agent files — not for the public GitHub repo
CLAUDE.md
CONTEXT.md
USERMANUAL.md
WRAPUP_PROTOCOL.md
ARCHITECTURE.md
GOODLUCK.md
.claude/

# Graphify knowledge graph — local only, regenerate with: graphify update .
graphify-out/
.graphifyignore
